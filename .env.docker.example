# Optional: override base images when Docker Hub is unreachable.
# Examples below use placeholder values. Replace them with images from a registry
# that your machine can access, then run `docker compose up -d --build` again.

BACKEND_PYTHON_IMAGE=python:3.11-slim
FRONTEND_NODE_IMAGE=node:22-alpine
FRONTEND_NGINX_IMAGE=nginx:1.27-alpine