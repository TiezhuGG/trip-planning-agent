# Docker image overrides for environments where Docker Hub is unreachable.
# Note: Docker image references must not include https:// or angle brackets.
# For official images on a custom registry, using the library/ namespace is the safest option.

BACKEND_PYTHON_IMAGE=python:3.11-slim
FRONTEND_NODE_IMAGE=node:22-alpine
FRONTEND_NGINX_IMAGE=nginx:1.27-alpine